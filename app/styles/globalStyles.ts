
import { StyleSheet } from 'react-native';

export const globalStyles = StyleSheet.create({
  h1: {
    fontSize: 40, // équivalent à 2.5em
    fontWeight: 'bold',
    color: '#333',
    marginBottom: 8, // équivalent à 0.5em
  },
  h2: {
    fontSize: 32, // équivalent à 2em
    fontWeight: 'bold',
    color: '#444',
    marginBottom: 8,
  },
  h3: {
    fontSize: 28, // équivalent à 1.75em
    fontWeight: 'bold',
    color: '#555',
    marginBottom: 8,
  },
});